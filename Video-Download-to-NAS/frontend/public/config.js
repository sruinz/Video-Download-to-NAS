// Runtime configuration
// This file can be modified after deployment without rebuilding
window.APP_CONFIG = {
  API_URL: '' // Empty means use same origin (default)
  // Users can set this to their backend URL if needed
  // Example: API_URL: 'https://api.example.com'
};
